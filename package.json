{
  "name": "nexus-library-importer",
  "version": "1.0.0",
  "private": true,
  "description": "Chromium and Firefox extension for importing PC game libraries into Nexus Mods My games.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check background.js && node --check content.js && node --check matcher.js && node --check nexus-worker.js && node --check steam-library.js && node --check gog-library.js",
    "smoke:catalog": "node scripts/live-catalog-smoke.cjs"
  }
}
